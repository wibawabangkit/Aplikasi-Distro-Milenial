<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bangkit.distromilenial">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.SubMenu.KonfirmPembayaran" />
        <activity android:name=".Activities.Emergency.WAActivity" />
        <activity android:name=".Activities.Emergency.TokpedActivity" />
        <activity android:name=".Activities.Emergency.SosmedActivity" />
        <activity android:name=".Activities.Menu.RequestActivity" />
        <activity android:name=".Activities.Menu.ProfileActivity" />
        <activity android:name=".Activities.Menu.ContactActivity" />
        <activity android:name=".Activities.Menu.BeliActivity" />
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.HomeActivity" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

</manifest>